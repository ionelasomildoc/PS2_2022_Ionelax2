#include <LiquidCrystal.h>
double time;
int last=0;
int addr=0;
String mesaj;
char msg[30]=" ";
void setup()
{ 
  Serial.begin(9600);
}

void loop(){
  String aux;
  int val1=0;
  int val2=0;
  time=millis();
  
  if(time>last+1000)
  {
  int T;
  val1=analogRead(A0);
  T=(val1*5000.0)/1023/10-50;
  Serial.print("3, ");
  Serial.println(T);
  last=millis();
  //delay(1000);
  }
  int i;
  
  if(Serial.available())
  {
    aux=Serial.readString();
    aux.toCharArray(msg, 32);
    if(aux[0]=='6')
       {Serial.println("mesaj");}
  }
}